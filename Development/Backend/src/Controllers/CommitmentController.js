/* src/controllers/commitmentController.js
   Controller com a lógica das rotas de "commitment".
   Exporta funções async que receberão (req, res) do Express:
   - createCommitment: criar um novo compromisso
   - getAllCommitments: listar todos
   - getCommitmentById: buscar por id
   - updateCommitment: atualizar por id
   - deleteCommitment: remover por id
   Cada função usa o model (src/models/Commitment.js) para operar no DB.
*/
