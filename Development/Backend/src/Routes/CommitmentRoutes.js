/* src/routes/commitmentRoutes.js
   Define as rotas HTTP relacionadas a "commitment" e mapeia para os controllers.
   Exemplo de rotas:
   - POST /create     -> createCommitment
   - GET /            -> getAllCommitments
   - GET /:id         -> getCommitmentById
   - PUT /:id         -> updateCommitment
   - DELETE /:id      -> deleteCommitment
*/
